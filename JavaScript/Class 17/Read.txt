Async => Callback -> Promise
-Better code (inversion of control)
- A Future event (resolve/reject in value)

{
value: undefined, value(data), error
status: pending, settled(resolved),rejected
}


Promises:
-Producer (fs : readfile, new Promise(-> fn))
-consumer (Success -> then , Failure -> catch , finally)


// Promise (static)

All -> all or none
All settled
Race -> the one that come first
Any -> first success

all of it stored in array

[p1,p2,p3,p4].all() - only if all promise are resolved or else rejected
[p1,p2,p3,p4].allsettled() -> 
